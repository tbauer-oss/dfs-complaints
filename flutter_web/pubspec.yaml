name: dfs_customer_complaint
description: DFS Customer Complaint Web App
environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  intl: ^0.19.0
  url_launcher: ^6.3.0
  flutter_localizations:
    sdk: flutter
  provider: ^6.1.2
  file_picker: ^10.3.3

flutter:
  uses-material-design: true
  assets:
    - assets/
  generate: true

flutter_intl:
  enabled: true

